export function TextInput({}) {
  return (
    <input 
      type="text" 
      style={{
        background: '#fff',
        borderRadius: '8px',
        padding: '8px',
        outline: 'none',
        border: 'none'
      }}/>
  )
}